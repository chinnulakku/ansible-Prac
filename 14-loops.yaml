- name: loops
  hosts: localhost
  become: yes
  tasks:
  - name: print install packages
    ansible.builtin.package:
    - name: "{{item}}"
      state: present
      loop:
      - git
      - mysql
      - postfix
      - nginx
    