- name: conditions
  hosts: localhosts
  tasks: 
  - name: check roboshop user exists or not
    ansible.buildin.command: id roboshop
    register: user #user is a variable name

  - name: print variable value
    ansible.builtin.debug:
      msg: "User output is : {{user}}" 